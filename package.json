{
  "name": "fridgetory",
  "version": "0.1.0",
  "private": false,
  "license": "MIT",
  "scripts": {
    "start": "next start -p $PORT",
    "dev": "next dev",
    "test": "yarn build",
    "build": "yarn prisma:generate && yarn nexus build && next build",
    "nexus:reflection": "nexus dev --reflection --entrypoint pages/api/graphql.ts",
    "nexus:check": "nexus build --entrypoint pages/api/graphql.ts && nexus build --entrypoint pages/api/playground.ts",
    "prisma:generate": "prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^2.0.1",
    "@apollo/client": "^3.0.0-rc.6",
    "antd": "^4.3.4",
    "apollo-cache-persist": "^0.1.1",
    "graphql": "^15.1.0",
    "next": "^9.4.2",
    "nexus": "^0.24.2",
    "nexus-plugin-prisma": "^0.10.0",
    "node-fetch": "^2.6.0",
    "react": "^16.13.1",
    "react-dom": "^16.13.1",
    "typescript": "^3.9.5"
  },
  "devDependencies": {
    "@prisma/cli": "^2.0.0-beta.7",
    "@semantic-release/git": "^9.0.0",
    "@semantic-release/release-notes-generator": "^9.0.1",
    "@types/node": "^14.0.13",
    "@types/react": "^16.9.41",
    "husky": ">=4",
    "lint-staged": "^10.2.11",
    "prettier": "^2.0.5",
    "semantic-release": "^17.0.8",
    "ts-node": "^8.10.2"
  },
  "prettier": {
    "semi": false,
    "singleQuote": true,
    "printWidth": 120
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "release": {
    "branches": [
      "master"
    ],
    "plugins": [
      "@semantic-release/commit-analyzer",
      "@semantic-release/release-notes-generator",
      "@semantic-release/git",
      "@semantic-release/github"
    ]
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx,yaml,css,md}": "prettier --write"
  }
}
