# Migration `20200516113333-init`

This migration has been generated by Adriaan Knapen at 5/16/2020, 11:33:33 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."ProductType" (
"id" SERIAL,"name" text  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."Product" (
"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"id" SERIAL,"placeId" integer  NOT NULL ,"productTypeId" integer  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."Item" (
"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"id" SERIAL,"productId" integer  NOT NULL ,"quantity" Decimal(65,30)  NOT NULL ,"unit" text  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."Place" (
"id" SERIAL,"name" text  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE UNIQUE INDEX "ProductType.name" ON "public"."ProductType"("name")

ALTER TABLE "public"."Product" ADD FOREIGN KEY ("productTypeId")REFERENCES "public"."ProductType"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."Product" ADD FOREIGN KEY ("placeId")REFERENCES "public"."Place"("id") ON DELETE CASCADE  ON UPDATE CASCADE

ALTER TABLE "public"."Item" ADD FOREIGN KEY ("productId")REFERENCES "public"."Product"("id") ON DELETE CASCADE  ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200516113333-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,37 @@
+datasource db {
+  provider = "postgresql"
+  url      = env("DATABASE_URL")
+}
+
+generator client {
+  provider = "prisma-client-js"
+}
+
+model ProductType {
+  id Int @default(autoincrement()) @id
+  name String @unique
+}
+
+model Product {
+  id Int @default(autoincrement()) @id
+  createdAt DateTime @default(now())
+  productType ProductType @relation(fields: [productTypeId], references: [id])
+  productTypeId Int
+  place Place @relation(fields: [placeId], references: [id])
+  placeId Int
+}
+
+model Item {
+  id Int @default(autoincrement()) @id
+  createdAt DateTime @default(now())
+  product Product @relation(fields: [productId], references: [id])
+  productId Int
+  quantity Float
+  unit String
+}
+
+model Place {
+  id Int @default(autoincrement()) @id
+  name String
+  products Product[]
+}
```
