image:
  file: .gitpod.Dockerfile

tasks:
  - init: >
      yarn install --frozen-lockfile &&
      yarn run build && 
      yarn run prisma:migrate
    command: yarn run start

ports:
  - port: 4000
    onOpen: open-preview
